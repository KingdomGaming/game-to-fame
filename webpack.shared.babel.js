export const ENV = {
    isProductionEnv: process.env.NODE_ENV === "production"
}

export const FILENAMES = {
    js: {
        entry: "main",
        output: "app"
    }
}

export const PLUGINS = [];
